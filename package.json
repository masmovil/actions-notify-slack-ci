{
  "name": "actions-notify-slack-ci",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "tsc && ncc build main.js -o dist && rm main.js",
    "package": "ncc build main.js -o dist",
    "test": "tsc && node test.js",
    "clean": "rm -f main.js && rm -rf dist/"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@slack/web-api": "^6.8.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/node-fetch": "^2.6.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^4.9.0"
  }
}
